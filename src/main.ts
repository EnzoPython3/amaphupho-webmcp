const app = document.getElementById('app');

if (app) {
  app.innerHTML = `
    <h1>Amaphupho</h1>
    <p>African dream insight and interpretation.</p>
    <p>This commit records the pre WebMCP integration baseline.</p>
  `;
}
